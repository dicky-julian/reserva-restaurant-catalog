import Home from '../views/pages/home';

const routes = {
  '/': Home,
//   '/now-playing': NowPlaying,
//   '/upcoming': Upcoming,
//   '/detail/:id': Detail,
//   '/like': Like,
};

export default routes;